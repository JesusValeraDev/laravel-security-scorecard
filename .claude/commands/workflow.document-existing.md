---
description: Reverse-engineer an existing capability into the repo's standard Spec Kit spec artifact.
argument-hint: "<existing-feature-or-flow-description>"
allowed-tools: Read, Grep, Glob, Bash, Edit, MultiEdit, Write, Task
disable-model-invocation: true
---

<!-- Generated by agnostic-ai -->

Run when the user wants to document or reverse-spec behavior that already exists in the codebase.

Required behavior:
1. Read the `code-to-spec` skill file and execute it end-to-end for the current repo and `$ARGUMENTS`.
2. Treat that skill as the canonical source of truth for reverse-spec behavior — do not duplicate or improvise the workflow here.
3. The final artifact must be the repo's standard `spec.md` in the active feature directory, using the repo's Spec Kit template — not a parallel custom template family or a sidecar `*-code-to-spec-*` document.
